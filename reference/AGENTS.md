# AGENTS.md

This directory contains preserved business reference material: workbooks, Office documents, and raw email examples.

Do not recursively scan or summarize this directory by default. Use it only when the user explicitly asks for reference material work, a spec points to a specific file, or a test fixture requires a specific example.

Keep the corpus tracked and intact. Do not delete, rename, convert, or normalize reference files unless the task explicitly requires that change.
